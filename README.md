# staggs_landscaping

Hi everyone I am currenly working on a web site for my uncle's landscaping business

# Installation

To run this project locally, you'll need Python 3.x installed on your machine.

## Clone the repository:

bash
Copy code
git clone https://github.com/your-username/your-project.git

## Navigate to the project directory:

cd your-project

## Create a virtual environment (recommended):

python3 -m venv venv
Activate the virtual environment:
venv\Scripts\activate (ps I am on a windows computer)
source venv/bin/activate

## Install dependencies:

pip install -r requirements.txt

## Usage

### Run the Flask application:

flask run
Open a web browser and visit http://localhost:5000 to view the application.
